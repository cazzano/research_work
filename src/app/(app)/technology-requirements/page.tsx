import { ContentDisplay } from '@/components/content/content-display';
import { techRequirementsData } from '@/lib/content.tsx';

export default function TechnologyRequirementsPage() {
  return <ContentDisplay topicData={techRequirementsData} />;
}
