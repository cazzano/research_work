import { ContentDisplay } from '@/components/content/content-display';
import { culturalImpactData } from '@/lib/content.tsx';

export default function CulturalImpactPage() {
  return <ContentDisplay topicData={culturalImpactData} />;
}
